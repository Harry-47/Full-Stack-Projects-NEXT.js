import VerifyModal from "../../components/feature/verify/VerifyModel/VerifyModel";

export default function VerifyPage() {
  return (

    //Shows a modal with the verification form when the user clicks on the "Start Verification" button in the dashboard. 
    //The modal allows users to upload images for verification and displays the results after processing.
      <VerifyModal />
  );
}
